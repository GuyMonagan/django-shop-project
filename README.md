# Django Shop Project

В проекте сделаны некоторые модернизации, детали которых будут внесены позже (это сообщение оставлено для отладки)

Простое учебное Django-приложение интернет-магазина, реализованное с использованием PostgreSQL, моделей, админ-панели, ORM-запросов, фикстур и кастомной команды.

В проекте реализованы:
- Модели `Product` и `Category` с описанием и связями.
- Поддержка изображений товаров (медиафайлы).
- Админка с настройкой отображения, фильтрацией, поиском и предпросмотром изображений.
- Импорт данных через фикстуры.
- Кастомная команда загрузки тестовых данных.
- Подключение к PostgreSQL через .env. 
- Загрузка изображений с настройками MEDIA_ROOT / MEDIA_URL. 
- Главная страница со списком товаров (название, изображение, описание до 100 символов). 
- Страница одного товара — отображает полную информацию о товаре. 
- Базовый шаблон с Bootstrap и футером. 
- Подшаблон меню, используемый на всех страницах. 
- Все шаблоны используют наследование, код не дублируется. 
- Ссылки с главной страницы ведут на страницу товара и обратно.
- Перевод всех контроллеров приложения catalog на CBV (Class-Based Views). 
- Контроллер и шаблон страницы контактов также переведены на CBV. 
- Создано новое приложение blog, зарегистрировано в INSTALLED_APPS. 
- Реализована модель блоговой записи (BlogPost) с полями:
  - заголовок, содержимое, превью, дата создания, статус публикации, счётчик просмотров. 
- Полноценный CRUD (создание, просмотр, редактирование, удаление) для блоговых записей реализован на CBV. 
- Использование базового шаблона и подшаблона меню на всех страницах блога. 
- Реализована логика:
  - счётчик просмотров увеличивается при просмотре статьи; 
  - список статей отображает только опубликованные записи; 
  - после редактирования статьи происходит редирект на её страницу.
- Все URL-адреса для блога имеют префикс /blog/ и корректный нейминг.

## Стек

- Python 3.11+
- Django 5.x
- PostgreSQL
- Poetry
- Bootstrap (вёрстка)

## Запуск проекта

Клонируйте репо:
```
git clone https://github.com/GuyMonagan/django-shop-project
```

Перейдите в папку проекта:
```
cd django-shop-project/src
```
Установите зависимости:
```
poetry install
```
Создайте и настройте .env файл:

```
DB_NAME=shop_db
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

```
Примените миграции:
```
poetry run python manage.py migrate
```
Создайте суперпользвателя:

```
poetry run python manage.py createsuperuser
```
Запустите сервер:
```
poetry run python manage.py runserver
```

Убедитесь, что создана фикстура по пути:

```
catalog/fixtures/initial_data.json
```

Выполните кастомную команду:

```commandline
poetry run python manage.py load_test_data
```

## Лицензия

Учебный проект. Используется в рамках курса Skypro Python-разработчик
